image:
  tag: deck-builder-api-1.1.3
ingress:
  enabled: true
  className: nginx
  host: dev.dracolich.cc
  path: /dracolich/mtg-deck-builder/api/v0
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  tls:
    - hosts:
        - dev.dracolich.cc
      secretName: dev-dracolich-cc-tls
