image:
  tag: deck-builder-api-1.1.6
ingress:
  enabled: true
  className: nginx
  host: dev.dracolich.app
  path: /dracolich/mtg-deck-builder/api/v0
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  tls:
    - hosts:
        - dev.dracolich.app
      secretName: dev-dracolich-app-tls
