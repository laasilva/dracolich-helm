apiVersion: v2
name: ai-api
description: dracolich-ai-api deployment (internal-only — no Ingress)
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: dracolich-service
    version: "0.1.0"
    repository: "file://../../lib"
